doctype html
head
    meta(name='viewport' content='width=device-width, initial-scale=1')
    script(src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js")
    style.
        .box {
            position: fixed;
            top: 20px;
            left: 400px;
            z-index: 2;
        }
        .tip {
            background-color: #fffbce;
            padding: 8px;
            margin: 3.2em 14em 2em 0;
            display: table ;
            font-size: 12px;
        }
        span.title {
            background: #ffed00;
            padding: .6em 1.5em;
            font-weight: 600;
            top: -10px;
            left: -20px;
            position: relative;
        }
        ul, #myUL {
        list-style-type: none;
        }

        #myUL {
        margin: 0;
        padding: 0;
        }
        .caret {
        cursor: pointer;
        -webkit-user-select: none; /* Safari 3.1+ */
        -moz-user-select: none; /* Firefox 2+ */
        -ms-user-select: none; /* IE 10+ */
        user-select: none;
        }

        .caret::before {
        content: "\25B6";
        color: black;
        display: inline-block;
        margin-right: 6px;
        }

        .caret-down::before {
        -ms-transform: rotate(90deg); /* IE 9 */
        -webkit-transform: rotate(90deg); /* Safari */'
        transform: rotate(90deg);  
        }

        .nested {
        display: none;
        }

        .active {
        display: block;
        } 


body(style="width: 500px; background:" + jadeargument["background"])
    h3="Test download for Content Delivery Management Server"  
    form(method="post",id="submit_form",name="submit_form")   
        input(type="text",id="uuid",name="uuid",placeholder="Enter UUID to submit")  
        button(type="submit") Submit 
 
    br 
    p="Data in storage."
    textarea(style="word-wrap: break-word;",rows="40" cols="200")=jadeargument['readData']
     